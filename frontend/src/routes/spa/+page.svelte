<div class="spa-wrapper">
    <h1>SPA</h1>
</div>

<style>
    .spa-wrapper {
        --nav-height: 3rem;
        --gap: 1rem;
        --pg-height: clamp(500px, calc(100vh - var(--header-height)), 800px);
        height: var(--pg-height);
        display: grid;
        place-items: center;
    }
</style>